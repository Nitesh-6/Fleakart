# from store_db import main
from customer import exe
#
# main()
exe()
# import mysql.connector
#
# myconn = mysql.connector.connect(host='localhost', user='root', password='Anumula@123$', database='ecomm_db')
# cursor = myconn.cursor()
# cursor.execute("SELECT DISTINCT email_id, wallet FROM users")
# num_rows = 9
# while True:
#     rows = cursor.fetchmany(size=num_rows)
#     if not rows:
#         break
#     for row in rows:
#         print(row)
